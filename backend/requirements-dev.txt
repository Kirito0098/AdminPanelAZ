# Development & CI dependencies.
# Installs production deps plus tooling for tests and lint.
# Usage: pip install -r backend/requirements-dev.txt
-r requirements.txt

# Test runner (also listed in requirements.txt for in-panel pytest).
pytest==8.3.4

# HTTP client for API tests.
httpx==0.28.1

# Linter (matches CI and pre-commit).
ruff>=0.11,<1.0

# Git hook runner.
pre-commit>=4.0,<5.0
