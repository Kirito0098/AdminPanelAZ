# AdminPanelAZ — allow longer server_name values (portal.panel.example.com, etc.).
# Power of two; nginx default is often 32/64 and fails nginx -t on longer FQDNs.
server_names_hash_bucket_size 128;
server_names_hash_max_size 1024;
