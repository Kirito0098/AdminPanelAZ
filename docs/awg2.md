# AZ-AWG2

## Зачем эта страница

**AZ-AWG2** управляет параллельным слоем **AmneziaWG 2.0** ([az-awg2](https://github.com/blindtechnique/az-awg2)) на активном VPN-узле: статус установки, создание и удаление клиентов, скачивание профилей. Штатные OpenVPN / WireGuard / стоковый AmneziaWG AntiZapret **не меняются**.

## Кто может пользоваться

| Действие | Администратор | Пользователь |
|----------|:-------------:|:------------:|
| Страница `/awg2` | ✓ | — |
| Создание / удаление на `/awg2` | ✓ | — |
| Создание / скачивание своих конфигов на **Конфигурации** | ✓ | ✓ (если модуль и visibility `amneziawg2`) |

## Как открыть

Боковое меню → **AZ-AWG2** (модуль `awg2` должен быть включён).

Если слой не установлен — страница покажет SSH-команду и кнопку **«Установить из панели»** с live-логом.
`install-base` и возможная перезагрузка по-прежнему остаются только в SSH.

### Telegram / Mini App

| Канал | Возможности |
|-------|-------------|
| Бот `/awg2` (admin) | Краткий статус слоя: installed, online/peers, top-3 трафика; кнопка в «⋯ Ещё» рядом с AZ-WARP |
| Mini App `/awg2` (admin) | Read-only карточки (health + мониторинг); **без** install / обфускации / backup |
| Mini App → Конфиги | Create / download / delete `amneziawg2` при toggle `awg2` и visibility; опциональный TTL |

Модуль `awg2` выключен → команда и пункт Mini App скрыты / disabled.

---

## Чем отличается

| | AZ-AWG2 | Стоковый AmneziaWG | AZ-WARP |
|--|---------|--------------------|---------|
| Назначение | VPN-профили AmneziaWG 2.0 (оба туннеля) | Профили AmneziaWG из AntiZapret | Точечный обход через Cloudflare WARP |
| Тип в панели | `amneziawg2` | файлы протокола `amneziawg` у `wireguard` | домены / IP, не VPN-клиент |
| Данные на диске | `/opt/antizapret-awg` | `client/amneziawg` AntiZapret | конфиги WARP |
| Вкладка Dashboard | **AmneziaWG 2.0** | **AmneziaWG** | — |

---

## Вкладки

| Вкладка | Для чего |
|---------|----------|
| **Клиенты** | Список `VpnConfig` типа `amneziawg2`, создание, TTL, удаление, скачивание |
| **Обфускация** | Текущий профиль, regenerate / apply (`awg-obfuscation`); после apply — переимпорт профилей |
| **Мониторинг** | Карточки интерфейсов и таблица клиентов (online / handshake / traffic) |
| **Бэкап** | Узкий backup/restore только для слоя AZ-AWG2 (`amneziawg`, клиенты, runtime) |
| **Справка** | Отличия от стока и AZ-WARP, команды install/update, HA / stats.db |

---

## Пошаговые сценарии

### Включить модуль

1. **Настройки → Модули** → включите **AZ-AWG2** (`awg2`)
2. При необходимости добавьте протокол `amneziawg2` в видимость профилей для пользователей
3. Откройте **AZ-AWG2** в меню

### Установить слой на сервере

1. Откройте **AZ-AWG2**
2. Либо нажмите **«Установить из панели»** и дождитесь live-лога, либо скопируйте SSH-команду ниже
3. Если нужна базовая установка AntiZapret (`install-base`) или ожидается перезагрузка, выполните шаг только по SSH на **активном** VPN-узле
4. Обновите статус на странице — должен появиться «установлен»

Команда установки:

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/blindtechnique/az-awg2/main/install.sh)
```

Обновление (без смены обфускации):

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/blindtechnique/az-awg2/main/install.sh) --update
```

### Создать клиента (админ)

1. Вкладка **Клиенты** на `/awg2` **или** **Конфигурации → Новый клиент → AmneziaWG 2.0**
2. Укажите имя (латиница, цифры, `_`, `-`, до 32 символов)
3. При необходимости задайте TTL: `нет` / `30m` / `2h` / `6h` / `7d`
4. Создайте — появятся профили обоих туннелей (`antizapret` и `vpn`)

Опция **AmneziaWG 2.0** в диалоге создания на Dashboard видна только если модуль включён и слой установлен на активном узле.
Если TTL задан, на карточках `/awg2` и Dashboard появится бейдж `истекает …`, а backend сохранит `expires_at`.

### Как работает автоудаление по TTL

Раз в 60 секунд панель по каждому узлу с клиентами `amneziawg2`:

1. запускает `awg-client expire-check` на узле;
2. читает `/opt/antizapret-awg/expiry.tsv` и обновляет `expires_at` в БД (TTL, заданный по SSH или приехавший с HA/restore, тоже показывает бейдж);
3. удаляет клиента и строку `VpnConfig` **только если срок уже истёк** — тем же HA-путём, что и `DELETE /api/configs/{id}` (реплики и shadow-строки чистятся).

Строка в панели **не** удаляется просто потому, что клиента нет на диске: восстановление старого backup не стирает клиентов, созданных после снятия backup. Ошибка одного узла (недоступен, слой не установлен) не мешает обработке остальных.

### Backup / Restore слоя

1. Откройте вкладку **Бэкап** на `/awg2`
2. Для экспорта нажмите **«Скачать backup»**
3. Для восстановления выберите архив `az-awg2-backup.tar.gz` через **«Загрузить и восстановить»**

Это **узкий** backup только для AZ-AWG2. Он не заменяет:

- `awg-backup`
- общий backup AntiZapret
- backup панели

### Скачать конфиг (пользователь)

1. **Конфигурации** → вкладка **AmneziaWG 2.0**
2. Скачайте нужный `.conf` (или QR, если модуль QR включён)

Стоковая вкладка **AmneziaWG** эти конфиги не показывает.

### Мониторинг клиентов

1. Откройте вкладку **Мониторинг** на `/awg2`
2. Смотрите карточки интерфейсов (порт, subnet, число пиров) и таблицу клиентов
3. Online ≈ последний handshake младше **180 секунд**
4. Источник: `awg_stats.py overview` при наличии `/opt/antizapret-awg/stats.db`; иначе live `awg show dump` (без 500)
5. Клик по строке клиента (или **Статистика** на вкладке **Клиенты**) открывает drawer: endpoint, GeoIP (если MMDB в `data/geoip/`) и дневной трафик

### Блокировка клиента

На Dashboard (вкладка **AmneziaWG 2.0**) и на `/awg2` → **Клиенты**:

- **Временная блокировка** — peer снимается с runtime, срок в policy DB
- **Блокировать навсегда** — до ручной разблокировки
- **Снять блокировку** — восстановление peer в `awg`

На HA replica mutating-действия скрыты (как для WireGuard).

---

## HA (синхронизация узлов)

В срезе 2a клиенты `amneziawg2` реплицируются в HA как crypto-sync: с primary копируются `/etc/amnezia/amneziawg` и `/opt/antizapret-awg/clients`, при архивировании исключаются `stats.db`, `venv/` и `__pycache__/`.

Если на replica нет слоя `az-awg2`, синхронизация завершается ошибкой и страница показывает `install_command`.

Применяется byte-copy + `apply_runtime`, а не `awg-client add` на replica.

`stats.db` остаётся **локальным** на каждом узле и в HA-архив не уезжает.

---

## Частые вопросы

**Нужен ли AZ-AWG2 всем?**  
Нет. Модуль opt-in (по умолчанию выключен). Включайте, если нужен слой AmneziaWG 2.0 рядом со стоком.

**Можно ли то же имя, что у WireGuard?**  
Да: уникальность `(узел, имя, тип VPN)` — `amneziawg2` и `wireguard` не конфликтуют.

**Где документация upstream?**  
[blindtechnique/az-awg2](https://github.com/blindtechnique/az-awg2)

---

## Будьте осторожны

- Не прерывайте установку/обновление слоя на сервере
- Не рассчитывайте на UI для `install-base` или reboot: это только SSH
- Не путайте вкладки **AmneziaWG** и **AmneziaWG 2.0** на Dashboard
- Для HA-копии AWG2 учитывайте byte-copy crypto-sync и исключения архива из среза 2a
- После смены обфускации переимпортируйте клиентские профили

---

[← Все руководства](README.md)
