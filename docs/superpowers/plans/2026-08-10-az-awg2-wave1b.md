# AZ-AWG2 Wave 1b Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans.  
> **Spec:** `docs/superpowers/specs/2026-08-10-az-awg2-wave1b-design.md`  
> **Depends on:** Wave 1a complete.  
> **Next:** `docs/superpowers/plans/2026-08-10-az-awg2-wave1c.md`  
> **Detail reference:** `docs/superpowers/plans/2026-08-10-az-awg2.md` Tasks 2, 3 (VpnType/guards/visibility), 4 (client agent), 5, 6, ClientsTab part of 7.

**Goal:** Full AWG2 client lifecycle on `/awg2` via `awg-client` + `VpnConfig(amneziawg2)` with HA replicate **skipped**.

**Architecture:** Extend `Awg2Service` with add/del/profiles; branch `/api/configs`; Clients tab; import + HA skip.

**Tech Stack:** FastAPI, SQLAlchemy, React, pytest.

## Global Constraints

- Both tunnels on create; rollback on partial failure  
- HA: **do not** call `maybe_replicate_*` for `amneziawg2`  
- No Dashboard tab yet (1c)  
- Skip commits unless user asks

## File map

| File | Role |
|------|------|
| `awg2.py` | add/del/profiles |
| `models.py` | `VpnType.amneziawg2` |
| `feature_guards.py` / `vpn_profile_visibility.py` | awg2 create guards |
| `configs.py` / `client_sync.py` | CRUD + HA skip |
| `config_import.py` | orphans + stale-safe |
| `node_adapter` / `node_agent` | client methods |
| `ClientsTab.tsx` | UI |
| tests | service CRUD, API create, HA skip, import, visibility |

---

### Task 1: add/del/profiles + tests

Implement parent plan Task 2 (flock, both tunnels, rollback, profile paths).

- [ ] Failing tests  
- [ ] Implement  
- [ ] PASS `test_awg2_service.py`

### Task 2: VpnType + guards + visibility

- [ ] Enum `amneziawg2`; `require_vpn_type`; PROTOCOLS + `can_create_vpn_type`  
- [ ] PASS visibility + require tests

### Task 3: Adapter/agent client methods + profile branch

- [ ] `awg2_add_client` / `awg2_delete_client` / list / `get_profile_files` branch  
- [ ] Parity test update  
- [ ] PASS

### Task 4: configs create/delete + HA skip

- [ ] Explicit branches; 409 if not installed  
- [ ] Skip replicate; defense in `maybe_replicate_*`  
- [ ] Tests: create calls awg2; replicate not called  
- [ ] PASS

### Task 5: Disk import

- [ ] `list_amneziawg2_clients`; stale-safe `_is_on_disk`  
- [ ] PASS

### Task 6: ClientsTab on `/awg2`

- [ ] List/create/delete/download via existing config APIs  
- [ ] Hide when !health.installed  
- [ ] `npm run build`  

---

## Done when

1b criteria met. Hand off to **1c**.
