# AZ-AWG2 Wave 1a Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans.  
> **Spec:** `docs/superpowers/specs/2026-08-10-az-awg2-wave1a-design.md`  
> **Epic:** `docs/superpowers/specs/2026-08-10-az-awg2-epic.md`  
> **Next:** `docs/superpowers/plans/2026-08-10-az-awg2-wave1b.md`  
> **Detail reference:** full snippets for detect/health live in `docs/superpowers/plans/2026-08-10-az-awg2.md` Tasks 1, 3 (toggle only), 4 (health), 7 (shell).

**Goal:** Ship opt-in AZ-AWG2 shell: toggle, health/status API, `/awg2` page with install-prompt — no client CRUD yet.

**Architecture:** `Awg2Service` detect + health; `/api/awg2/health|status`; FeatureGuard + empty page.

**Tech Stack:** FastAPI, React, pytest.

## Global Constraints

- Toggle `awg2` / `FEATURE_AWG2_ENABLED` default **False**
- Label **AZ-AWG2**; path `/awg2`
- Install = show SSH command only
- No `VpnType.amneziawg2` create yet (1b)
- No HA / obfuscation / monitoring
- Skip commits unless user asks

## File map

| File | Role |
|------|------|
| `backend/app/services/awg2.py` | detect, health, thin status |
| `backend/app/routers/awg2.py` | GET health/status |
| `backend/app/services/feature_toggles.py` | register `awg2` |
| `backend/app/services/node_adapter.py` + `node_agent` | health/status parity |
| `backend/app/main.py` | mount router |
| `frontend/.../Awg2Page.tsx`, InstallPrompt, Help stub | UI shell |
| `Layout.tsx`, `App.tsx` | nav + route |
| tests | `test_awg2_service.py` (detect), `test_awg2_feature_toggle.py`, `test_awg2_api.py` (health) |

---

### Task 1: Detect + health service + tests

Copy/implement from parent plan Task 1 (`detect_awg2_installation`, `get_health`, `list_clients` dir-scan ok for status counts). **Do not** implement `add_client`/`delete_client` yet (stub or omit).

- [ ] Failing tests for detect/health  
- [ ] Implement  
- [ ] PASS: `pytest tests/test_awg2_service.py -q` (detect/health only)

### Task 2: Feature toggle `awg2`

- [ ] Register toggle default False, prefixes `/api/awg2`, path `/awg2`, resource profiles key  
- [ ] Tests: default off; path blocked when disabled  
- [ ] PASS

### Task 3: Router + adapter + agent (health/status only)

- [ ] `routers/awg2.py` GET health/status; mount in `main.py`  
- [ ] `get_awg2_health` / `get_awg2_status` on Local/Remote + agent  
- [ ] API test health with toggle on  
- [ ] PASS

### Task 4: Frontend shell

- [ ] Types + `getAwg2Health` / `getAwg2Status`  
- [ ] `Awg2Page` + `Awg2InstallPrompt` + short Help («клиенты — срез 1b»)  
- [ ] Layout nav + App route + `haNodeScope` test  
- [ ] `npm run build`  
- [ ] PASS

### Task 5: Minimal docs note

- [ ] Short stub in CHANGELOG or comment in PROJECT_MAP that module shell landed; full `docs/awg2.md` in 1c  

---

## Done when

1a criteria in design met. Hand off to **1b**.
